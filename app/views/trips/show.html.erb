<h1>Trip Details</h1>

<ul>
  <li>Date: <%= @trip.date %></li>
  <li>Rating: <%= @trip.rating %></li>
  <li>Cost: <%= @trip.cost %></li>
  <li>Driver: <%= @trip.driver_id %></li>
  <li>Passenger: <%= @trip.passenger_id %></li>
</ul>

<p><%= link_to "Edit This Trip", edit_trip_path(@trip) %></p>
<p><%= link_to "Delete This Trip", trip_path(@trip), method: :delete, data: { confirm: "Are you sure you want to delete this trip?" } %></p>
<p><%= link_to "See All Trips", trips_path %></p>