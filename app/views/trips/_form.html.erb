<%= form_with model: @trip, class: 'trip-form' do |f| %>
  <p>Please fill in the form to update our database:</p>

  <p><%= f.label :rating %>
    <%= f.select :rating, [1, 2, 3, 4, 5] %></p>

  <p><%= f.label :cost %>
    <%= f.text_field :cost %></p>

  <p><%= f.label :date %>
    <%= f.text_field :date %></p>

  <p> <%= f.submit action_name, class: 'trip-button' %></p>
<% end %>