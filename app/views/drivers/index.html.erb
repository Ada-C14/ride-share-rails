<h1>DRIVERS</h1>

<table style="width:50%">
  <tr>
    <th>Driver ID</th>
    <th>VIN</th>
    <th>Status</th>
  </tr>
  <% @drivers.each do |driver| %>
    <tr>
      <td><%= "#{driver.id}"%> </td>
      <td><%= "#{driver.vin}" %> </td>
      <td><%= driver.available ? "Available" : "Unavailable" %> </td>
    </tr>

  <% end %>
</table>
