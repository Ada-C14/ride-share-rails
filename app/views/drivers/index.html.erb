<h1>DRIVERS</h1>

<table style="width:50%">
  <tr>
    <th>Driver ID</th>
    <th>VIN</th>
    <th>Available?</th>
  </tr>
  <% @drivers.each do |driver| %>
    <tr>
      <td><%= link_to "#{driver.id}", driver_path(driver.idO)%> </td>
      <td><%= "#{driver.vin}" %> </td>
      <td><%= driver.available ? "Available" : "Unavailable" %> </td>
    </tr>

  <% end %>
</table>
