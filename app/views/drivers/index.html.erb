<h1> Driver list </h1>
<nav>
  <%=link_to "Homepage", homepages_path %> |
  <%=link_to "View all riders", passengers_path %> |
  <%= link_to "Add a New Driver", new_driver_path%>
</nav>

<div>
  <%@drivers.each do |driver|%>
    <ul>
      <li><strong><%= link_to driver.name, driver_path(driver.id) %></strong> : Driver ID: <%= driver.id%> | VIN: <%= driver.vin%> | <%= link_to "Edit Driver", edit_driver_path(driver.id) %> | <%= link_to "Delete Driver", driver_path(driver.id), method: :delete, data: {confirm: "Are you sure?"} %> |</li>
    </ul>
  <%end%>
</div>
<nav>
  <%=link_to "Homepage", homepages_path %> |
  <%= link_to "Add a New Driver", new_driver_path%> |
</nav>


