<%= form_with model: @driver, class: 'driver-form' do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :vin %>
  <%= f.text_field :vin %>

  <%#= f.label :available %>
  <%#= f.text_field :available %>

  <%= f.submit "Save", class: "button" %>
<% end %>