<%= form_with model: @driver do |f| %>

  <%= f.label :vin %>
  <%= f.text_field :vin%>
  <br>
  <%= f.label :available %>
  <%= f.select :available, [ true, false ] %>
  <br>
  <br>
  <%= f.submit "#{button_name}" %>
<% end %>