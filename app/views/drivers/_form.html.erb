<%= form_with model: @driver do |f| %>
  <div>
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>
  <div>
    <%= f.label :vin %>
    <%= f.text_field :vin %>
  </div>
  <%= f.hidden_field :available, :value => true %>
  <%= f.submit "#{action_name} Driver" %>
<% end %>