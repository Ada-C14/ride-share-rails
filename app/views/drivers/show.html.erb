<h1><%= @driver.name %></h1>
<% if @driver.available %>
  <p><strong>AVAILABLE</strong></p>
  <% else %>
  <p><strong>NOT AVAILABLE</strong></p>
  <% end %>
<p>VIN: <%= @driver.vin %></p>

<h3>List of Trips</h3>
<table>
  <thead>
  <tr>
    <th>ID</th>
    <th>Date</th>
    <th>Passenger</th>
    <th>Rating</th>
    <th>Earnings</th>
    <th>Delete</th>

  </tr>
  </thead>
  <tbody>
  <% @driver.trips.each do |trip| %>
  <tr>
      <td><%= link_to "#{trip.id}", trip_path(trip) %></td>
      <td><%= trip.date.strftime("%m/%d/%Y") %></td>
      <td><%= link_to "#{trip.passenger.name}", passenger_path(trip.passenger) %></td>
      <td><%= trip.rating %></td>
      <td>$<%= @driver.single_trip_earnings(trip) %></td>
      <td><%= link_to "Delete", trip_path(trip), method: :delete, data: {confirm: "Are you sure?"} %></td>
  </tr>
  <% end %>
  <tfoot>
    <tr>
      <td></td>
      <td></td>
      <td></td>
      <td>Average: <%= @driver.average_rating %></td>
      <td>Total: $<%= @driver.total_earnings %></td>
      <td></td>
    </tr>
  </tfoot>
  </tbody>
</table>



<%= link_to "Edit", edit_driver_path(@driver) %>
<%= link_to "Delete", driver_path(@driver), method: :delete, data: {confirm: "Are you sure?"} %>

