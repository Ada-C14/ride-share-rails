<h2>Driver Details</h2>

<div><strong>Driver: <%= @driver.name %></strong>
  <p><strong>Vin: </strong><%= @driver.vin %></p>
  <p><strong>Availability:</strong> <%= @driver.available %></p></div>

<!--  <p class="buttons"><%#= link_to "Edit Driver", edit_driver_path(@driver.id) %></p>-->
<!--  <p class="buttons" id="delete_driver"><%#= link_to "Delete Driver", delete_driver_path(@driver.id), method: :delete, data: {confirm: "Are you sure you want to delete this driver?"} %></p>-->

  <p class="buttons"><%= link_to "Back to Index", drivers_path %></p>
  <p class="buttons"><%= link_to "Edit Driver Info", edit_driver_path(@driver.id) %></p>
  <p class="buttons" id="delete"><%= link_to "Delete Driver Info", delete_driver_path(@driver.id) %></p>