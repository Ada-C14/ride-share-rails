<h2><%= @driver.name%></h2>

<h4>List of Trips:</h4>
<ul>
  <% @driver.trips.each do |trip| %>
    <li>Trip ID: <%= link_to trip.id, trip_path(trip.id) %> Trip Date: <%= trip.date %></li>
  <% end %>
</ul>

<div><%= link_to "Back to Drivers", drivers_path %></div>

<div><%= link_to "Edit Driver", edit_driver_path %></div>