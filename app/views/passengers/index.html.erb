<h1>passengers</h1>

<%= link_to "Create New Passenger Account", new_passenger_path %>


<% @passengers.each do |passenger|%>
  <li>
    <%=passenger.name %> | <%= link_to "Details", passenger_path(passenger.id) %>
  </li>
<% end %>