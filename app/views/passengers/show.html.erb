<h2>Passenger Details</h2>

<ul>
  <li>Name: <%= @passenger.name %></li>
  <li>Phone number: <%= @passenger.phone_num %></li>
  <li>Total charges: $<%= sprintf('%.2f', @passenger.total_cost/100) %></li>
</ul>

<%= link_to "Edit Passenger Details", edit_passenger_path(@passenger.id) %>

<h4>Trips: </h4>

<ul>
  <% @passenger.trips.each do |trip| %>
  <li> ID: <%= link_to trip.id, trip_path(trip) %> --
    Driver: <%= trip.driver.name %> --
    Passenger: <%= trip.passenger.name %> --
    Cost: $<%= sprintf('%.2f', trip.cost/100) %> --
    Rating: <%= trip.rating %>
  </li>
  <% end %>
</ul>

<%= link_to "Request Trip", new_passenger_trip_path(@passenger.id) %>