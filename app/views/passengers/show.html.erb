<strong><%= @passenger.name %></strong>

<ul>
  <br>
  <li>Passenger Id: <%= @passenger.id %></li>
  <li>Phone Number: <%= @passenger.phone_num %></li>
  <li>Total Charged: $<%= @passenger.total_spent %></li>
</ul>

<p>List of Trips:</p>

<ol>
<% if @passenger.trips.empty? %>
    <p><strong>Trips list coming soon!</strong></p>
<% else %>
  <% @passenger.trips.each do |trip| %>
      <li><%= link_to "#{trip.date}", trip_path(trip.id) %></li>
  <% end %>
    </ol>
<% end %>

  <%#= button_to "Request trip for #{@passenger.name}", passengers_trip_path(@passenger.id) %>

  <%= button_to "Edit Passenger", edit_passenger_path(@passenger),
                method: "get", class: "button" %>

  <%= button_to "Delete Passenger", passenger_path(@passenger),
                method: :delete, class: "button",
                data: {confirm: "WARNING: You cannot get this record back. Delete?"} %>

  <%= button_to "Return to Passenger List", passengers_path,
                method: "get", class: "button" %>

