<h2>Passenger Details</h2>

<div><strong>Passenger: <%= @passenger.name %></strong>
  <p><strong>Vin: </strong><%= @passenger.phone_num %></p></div>

<!--  <p class="buttons"><%#= link_to "Edit Passenger", edit_passenger_path(@passenger.id) %></p>-->
<!--  <p class="buttons" id="delete_passenger"><%#= link_to "Delete Passenger", delete_passenger_path(@passenger.id), method: :delete, data: {confirm: "Are you sure you want to delete this passenger?"} %></p>-->

  <p class="buttons"><%= link_to "Back to Index", passengers_path %></p>
  <p class="buttons"><%= link_to "Edit Passenger Info", edit_passenger_path(@passenger.id) %></p>
  <p class="buttons" id="delete"><%= link_to "Delete Passenger Info", passenger_path(@passenger.id), method: :delete, data: {confirm: "Are you sure?"} %></p>