<%= form_with model: @passenger, class: "create-passenger" do |f|  %>
  <p> Please provide or change for following information </p>

  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :phone_num %>
  <%= f.text_field :phone_num %>

  <%= f.submit "#{action_name} Passenger", class: "passenger-button" %>

<% end %>